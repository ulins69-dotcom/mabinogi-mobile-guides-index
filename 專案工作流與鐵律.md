# 瑪奇攻略查詢網 — 專案工作流與鐵律

> 本文件是全案的最高依循準則。任何程式、爬蟲、代理人的產出，都必須先通過這份文件的檢驗。文件與實作衝突時，先修正到一致，再往下走。
> 版本：v1.0 ｜ 建立日期：2026-08-23

---

## 零、專案定位（一句話）

一個**唯讀、靜態、能自動更新**的瑪奇 Mobile 攻略索引站：把巴哈姆特、YouTube（未來含官網公告）的攻略集中、分類、標記精華，讓玩家在一頁之內找到所有攻略。

判斷任何功能該不該做，都回到這句話：**它有沒有幫玩家更快找到攻略?** 沒有，就不做。

---

## 一、架構決策（已定案，不再重複討論）

| 項目 | 決策 | 原因 |
|---|---|---|
| 前端 | 單一 `index.html`（HTML + CSS + JS 同檔） | 唯讀站，單檔最好維護 |
| 資料 | 靜態 `guides.json` | 不需即時查詢，靜態檔最快、最省 |
| 部署 | GitHub Pages / Netlify | 免費、零維運、自帶 HTTPS |
| 資料生產 | 排程型「資料管線代理人」（Python + AI） | 爬取與策展是重複勞動，交給代理人 |
| ~~PostgreSQL~~ | **不使用** | 唯讀站無寫入需求，資料庫是過度設計 |
| ~~Node.js / Express~~ | **不使用** | 沒有動態 API 需求，後端是多餘的一層 |

> 原始 MD 宣稱的 PostgreSQL + Express 架構為「紙上架構」，實際程式只讀靜態 JSON。此決策正式將架構收斂為靜態站，文件即日對齊。

---

## 二、資料合約（Schema）— 全案唯一的接點

`guides.json` 是網站（消費端）與代理人（生產端）之間**唯一的契約**。兩邊都只認這份結構。

```json
{
  "updated_at": "2026-08-23",
  "guides": [
    {
      "id": "inven-123456",
      "title": "法師轉職完全攻略",
      "title_original": "법사 전직 완벽 공략",
      "author": "인벤",
      "category": "職業解析",
      "tags": ["法師", "轉職", "技能"],
      "url": "https://mabimo.inven.co.kr/...",
      "summary": "一句話重點摘要",
      "key_points": ["符文A搭配裝備B可提升20%輸出", "第二階段BOSS會轉向要繞背"],
      "source": "inven",
      "region": "kr",
      "published_at": "2026-08-01",
      "is_featured": true,
      "thumbnail": "https://..."
    }
  ]
}
```

欄位規範（v2 台韓雙軌）：

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---|---|
| `id` | string | ✅ | 唯一鍵，格式 `{source}-{原站ID}`，用於去重 |
| `title` | string | ✅ | 顯示標題（一律中文；韓服為 AI 翻譯後） |
| `title_original` | string | ✅ | 原始標題（韓服為韓文原文，台服同 title） |
| `author` | string | ✅ | 作者，抓不到填 `"未知"` |
| `category` | string | ✅ | 限定值：`新手指南`／`職業解析`／`副本攻略`／`活動情報` |
| `tags` | string[] | ✅ | 語意標籤，至少為空陣列 `[]`，不可為 null |
| `url` | string | ✅ | 原文連結 |
| `summary` | string | ❌ | 中文摘要，可空字串 |
| `key_points` | string[] | ✅ | AI 從內文摘要抽出的 0~3 條具體重點，至少為空陣列 `[]`，不可為 null |
| `source` | string | ✅ | 限定值：`bahamut`／`youtube`／`inven`／`nexon`／`official` |
| `region` | string | ✅ | 限定值：`tw`（台服）／`kr`（韓服搶先） |
| `published_at` | string | ✅ | `YYYY-MM-DD` |
| `is_featured` | boolean | ✅ | 是否精華 |
| `thumbnail` | string | ❌ | 縮圖網址，可空 |

> **v2 變更（2026-08-23）**：加入韓服來源。新增 `region` 區分台/韓、`title_original` 保留原文；`title` 統一為中文顯示（韓文經 AI 翻譯）。前端以「台服／韓服搶先」分頁呈現，兩者不混。
> **策略意義**：韓服領先台服約 15 個月，韓服資料作為台服未來內容的「領先指標」。
> **AI 降級鐵則**：分類與翻譯用 Gemini；無 `GEMINI_API_KEY` 或呼叫失敗時，自動降級回規則版（韓文不翻譯），整條管線不得中斷。
> **v2.2 變更（2026-09-03）**：韓文標題翻譯新增獨立安全網 `scraper/translate.py`（Google Cloud Translation API），跟 Gemini 是不同服務、不同金鑰、不同配額。三層降級：Gemini 成功 → 用 Gemini 的翻譯（品質較好）；Gemini 失敗但翻譯安全網成功 → 用安全網的翻譯；兩者都沒有 → 才維持韓文原文。**新增的 GitHub Secret** `GOOGLE_TRANSLATE_API_KEY`（是一把全新的 Cloud Translation API 金鑰，**不是** `GEMINI_API_KEY`、不能共用，申請流程也不同——見下方步驟）：
> 1. 前往 [Google Cloud Console](https://console.cloud.google.com/)，建立或選一個專案（可以是既有專案，不用跟 Gemini 的 AI Studio 專案一樣），需要掛一個帳單帳戶（免費額度內不會扣款）
> 2. 在該專案「啟用的 API」搜尋並啟用 **Cloud Translation API**
> 3. 到「憑證」頁面「建立憑證」→「API 金鑰」，複製產生的金鑰字串
> 4. 到本專案 GitHub repo 的 Settings → Secrets and variables → Actions → **New repository secret**（這是新增一個全新的 Secret 項目，名稱填 `GOOGLE_TRANSLATE_API_KEY`，不是去編輯 `GEMINI_API_KEY` 或 `YOUTUBE_API_KEY` 既有的項目）
> **v2.1 變更（2026-09-03）**：新增 `key_points`。網站真正的價值不是內容量贏過同類站，是把「人寫的長文」濃縮成讀者一眼能掃完的具體重點——AI 從 `bahamut.py` 抓到的內文摘要抽 0~3 條，前端卡片優先顯示這個，取代原本一整行 `summary`；沒有 `key_points`（規則版降級、AI 抽不出東西）就自動退回顯示 `summary`，不開天窗，同套「AI 降級鐵則」。

### 附屬合約：`class_digests.json`（職業速答卡，2026-09-03 新增）

跟 `guides.json` 分開的獨立檔案，**不算改動上面鎖定的主 schema**——選職業後玩家要的是直接答案（符文選什麼、技能怎麼加點），不是又一批文章連結；這份檔案是 AI 跨篇合成多篇 `guides.json` 內容產出的結構化摘要，來源見 `scraper/class_digest.py`（Phase 2 才會實作，目前前端已經先寫好消費端邏輯，抓不到檔案時顯示「準備中」，不是空白或報錯）。

```json
{
  "updated_at": "2026-09-03",
  "digests": {
    "戰士": {
      "guide_count": 10,
      "runes": [{ "text": "...", "source_ids": ["bahamut-xxx", "youtube-yyy"] }],
      "skill_order": [{ "text": "...", "source_ids": [...] }],
      "faq": [{ "text": "...", "source_ids": [...] }]
    }
  }
}
```

- 頂層 key 只能是 `classify.py` 的 `OFFICIAL_CLASSES` 六大職業名稱之一。
- `runes`／`skill_order`／`faq` 都是「條目陣列」，每條目 `text`（繁中）+ `source_ids`（引用哪幾篇 `guides.json` 的 `id`，前端用來顯示「資料來自：《OO》」並連回原文——沒有依據的內容不寫，寧可陣列是空的）。
- `guide_count` 是這個職業當次合成依據的來源篇數，前端顯示在卡片下方，讓玩家知道這份摘要的依據厚不厚。
- 前端消費邏輯：[index.html](index.html) 的 `loadClassDigests()` / `renderClassDigest()`。抓不到檔案（404）或某職業沒有 entry，一律顯示「這個職業的重點整理還在準備中」，不當錯誤處理、不留空白。

---

## 三、完整工作流（三階段，循序推進）

### Phase 0 ─ 爬蟲可行性快篩（約半天）

**目的**：先確認「題目有解」，再投入工時。資料源拿不到，後面一切免談。

工作項：
1. 確認巴哈姆特瑪奇 Mobile 攻略版的頁面結構可否解析出標題／作者／日期／連結。
2. 確認 YouTube Data API v3 金鑰申請方式、免費配額、能否回傳影片清單。

**Definition of Done**：產出一份「可行／不可行」結論，兩個資料源各有明確答案與注意事項。

---

### Phase 1 ─ 重寫網站 + 鎖定 Schema（小時級）

**目的**：先做出乾淨、能上線、能正確顯示的**消費端**，作為日後檢驗代理人成果的鏡子。

工作項：
1. 修 P0：XSS 注入（改用 `textContent`／跳脫）、null 防護（每欄位給預設值）。
2. 依第二節 Schema 改寫資料載入邏輯（單一 `guides` 陣列）。
3. 補齊 空狀態／錯誤狀態／載入中 三種 UI。
4. 砍掉多餘的 `category.html`，分類篩選全部收進單頁。
5. 搜尋加 debounce。
6. 用一份範例 `guides.json` 在本機驗證顯示正常。

**Definition of Done**：單檔 `index.html` 可獨立運行；壞資料不會拖垮整頁；搜尋／分類／精華篩選全部正常；Schema 正式鎖定。

---

### Phase 2 ─ 設計資料管線代理人（天級）

**目的**：讓網站「活起來」，不用人天天維護。

流程：**爬取 → 去重 → AI 分類／打標／評精華 → 產出符合 Schema 的 `guides.json` → 自動部署**

| 環節 | 做法 |
|---|---|
| 爬取 | Python（requests + BeautifulSoup）抓巴哈；YouTube Data API 抓影片 |
| 去重 | 以 `id` 為鍵；AI 輔助判斷同主題重複 |
| 分類／打標 | **AI 讀內容判斷 category 與 tags**（這是 AI 勝過正則的關鍵） |
| 評精華 | **AI 依觀看數／留言／內容完整度綜合評分決定 `is_featured`** |
| 部署 | GitHub Actions 週排程，產檔後自動 push 觸發部署 |

**Definition of Done**：代理人可一鍵（或排程）跑完全流程，產出通過 Schema 檢驗的 `guides.json` 並自動上線；活動期間可手動觸發。

---

## 四、鐵律（不可違反，違反即回退重做）

1. **文件與實作必須一致。** 架構寫什麼，程式就跑什麼。發現不一致，先改到對齊再往下做，絕不放任「紙上架構」。

2. **資料合約神聖不可侵犯。** Schema 一旦鎖定，任一邊要改，兩邊必須同步，並更新本文件第二節。私自改 Schema = 破壞契約。

3. **消費端先於生產端。** 先有能正確顯示的網站，才做產資料的代理人。順序不可顛倒。

4. **外部資料一律視為敵意。** 所有爬來的字串輸出到頁面，一律用 `textContent` 或跳脫，**永不用 `innerHTML` 直接拼接外部資料**。

5. **每個欄位都要防呆。** 假設每筆資料都可能缺欄位。渲染前一律給預設值，**單筆壞資料不可拖垮整頁**。

6. **用不到的東西不做（YAGNI）。** 不為「未來可能」預先架資料庫、後端、框架。真正需要時再加。

7. **每階段有驗收標準，未達標不進下一階段。** 不允許三個階段同時開工、互相牽制。

8. **先驗證題目有解，再投入工時。** 資料源、API 金鑰、配額，動工前先確認拿得到。

9. **代理人只碰資料管線，不碰網站服務。** 職責單一：它只負責產出 `guides.json`，不介入前端。

10. **一切進版本控制，部署自動化。** 手動部署遲早出錯。程式、資料、設定全部進 Git，部署交給 CI。

11. **消耗稀缺外部資源的除錯，不能拿正式環境當試驗場。** API 每日配額、費率限制這類「用掉當天就沒了、而且我們控制不了何時恢復」的資源，改一版就觸發一次正式管線去驗證，等於在拿最貴的測試方式做最便宜的假設驗證。修這類問題前，先窮盡免成本的自我檢查（讀官方文件、讀實際錯誤訊息、本機用假金鑰測 route 是否存在、combining 多個假設一次改完），把有把握的部分排除掉，才用一次正式驗證去確認剩下的不確定性——不要「改一點、測一次」反覆試。本機零成本的驗證（瀏覽器前端功能、schema 驗證、unit test）不受此限，那些該多測就多測。（2026-09-02 血淚教訓：一晚跑了 6、7 輪含 AI 的正式管線在除錯 Gemini 端點，直接把當天配額燒光。）

---

## 五、目前進度

- [x] 架構決策定案（第一節）
- [x] 資料合約定義（第二節）
- [x] 工作流與鐵律成文（本文件）
- [ ] Phase 0：爬蟲可行性快篩
- [ ] Phase 1：重寫網站 + 鎖 Schema
- [ ] Phase 2：設計資料管線代理人
